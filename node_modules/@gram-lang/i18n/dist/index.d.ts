type LanguageDict = Record<string, string[]>;
type CompiledDict = Record<string, string>;
/** Compiles alias lists into O(1) lookup tables, per language plus a unified global fallback. */
declare function compileDictionary(dictionaries: Record<string, LanguageDict>): {
    byLang: Record<string, CompiledDict>;
    global: CompiledDict;
};

declare const TIME_DICTIONARIES: {
    en: LanguageDict;
    fr: LanguageDict;
};
declare const TIME_BY_LANG: Record<string, CompiledDict>;
declare const TIME_GLOBAL: CompiledDict;
declare const TIME_TO_MINUTES: Record<string, number>;
declare const resolveTimeUnit: (unit?: string | null, lang?: string) => string;

declare const UNIT_DICTIONARIES: {
    en: LanguageDict;
    fr: LanguageDict;
};
declare const UNIT_BY_LANG: Record<string, CompiledDict>;
declare const UNIT_GLOBAL: CompiledDict;
interface UnitMap {
    base: string;
    map: Record<string, number>;
}
declare const UNIT_CONVERSIONS: {
    mass: UnitMap;
    volume: UnitMap;
};
/**
 * Normalizes a raw string unit into a standard canon unit.
 * Examples: 'càs' -> 'tbsp', 'kilo' -> 'kg'.
 */
declare function normalizeUnit(rawUnit: string | undefined | null, lang?: string): string;
/**
 * True for a mass/volume unit at or above its category's base unit (g, kg,
 * oz, lb, ml, cl, dl, l, tsp, tbsp, cup, ...) — the kind of unit a cook
 * measures with a scale or a spoon, not a lab balance. Callers use this to
 * decide when a quantity should be rounded coarser for display than the
 * precision it's stored at. Sub-base units (mg, drop, smidgen, pinch, dash)
 * return false since their values are already small.
 */
declare function isCoarseUnit(rawUnit: string | undefined | null): boolean;

declare function getAiLanguageInstruction(lang?: string): string;

declare const CATEGORY_KEYS: readonly ["vegetables", "fruits", "meat", "seafood", "dairy", "grains", "legumes", "nuts", "oils", "herbs", "spices", "condiments", "sugars", "beverages", "other"];
type CategoryKey = (typeof CATEGORY_KEYS)[number];
/**
 * Display label for every category key, in canonical order — for showing to
 * a human (a prompt, a UI list). Never persist these as data; persist the
 * key (`CategoryKey`) instead, and translate to a label only at display time.
 */
declare function getCategoryLabels(lang?: string): Record<CategoryKey, string>;
declare function isCategoryKey(value: string): value is CategoryKey;
/**
 * Returns display labels only, in canonical key order. Kept for the one
 * legitimate label-only use left (`shopper.ts`'s sort-order comparison,
 * which also has to keep matching pre-F-03 databases that already have a
 * translated label — not a key — written into `category`). Prefer
 * `getCategoryLabels`/`CATEGORY_KEYS` for anything that persists a category
 * as data.
 */
declare function getDefaultCategories(lang?: string): string[];

declare const en: {
    renderer: {
        shoppingList: string;
        cookware: string;
        equipment: string;
        nutrition: string;
        totalTime: string;
        idleTime: string;
        activeTime: string;
        prepTime: string;
        totalTimeTooltip: string;
        idleTimeTooltip: string;
        activeTimeCardTooltip: string;
        prepTimeTooltip: string;
        est: string;
        passiveTimeTooltip: string;
        activeTimeTooltip: string;
        estimateTooltip: string;
        coverageTooltip: string;
        unknownCookware: string;
        reset: string;
        originalRecipe: string;
        sectionStandardizedMass: string;
        bakersPercentage: string;
        standardized: string;
        optional: string;
        calculationPartialOrFailed: string;
        ingredientsOverhead: string;
        cookwareOverhead: string;
        breakdownActive: string;
        breakdownPassive: string;
        breakdownTimer: string;
        breakdownPrep: string;
        standardizedMass: string;
        quantity: string;
        intermediateIngredient: string;
        alternatives: string;
        decreasePortions: string;
        editPortions: string;
        increasePortions: string;
        returnToStep: string;
        mixedUnitsTooltip: string;
        mixedUnits: string;
        purchasingWeight: string;
        gross: string;
        intermediateResult: string;
        moduleFrom: string;
        estimatedSuffix: string;
        missingMass: string;
        someIngredients: string;
        nutritionWholeRecipe: string;
        nutritionPerPortion: string;
        nutritionPer100g: string;
        nutritionRawMassNote: string;
        nutritionRawMassNotePartial: string;
        incompleteData: string;
        ofWhich: string;
        nutrients: {
            calories: string;
            fat: string;
            sat_fat: string;
            mono_fat: string;
            poly_fat: string;
            carbs: string;
            sugar: string;
            fiber: string;
            protein: string;
            sodium: string;
            alcohol: string;
        };
    };
    playground: {
        title: string;
        inputTab: string;
        writePlaceholder: string;
        outputTab: string;
        viewLabel: string;
        loadExample: string;
        recipe: string;
        scaleFactor: string;
        examples: {
            newRecipe: string;
            canneles: string;
            empanadas: string;
        };
        views: {
            json: string;
            ast: string;
            jsonTree: string;
            markdown: string;
            preview: string;
            gantt: string;
            gantt_active: string;
            gantt_passive: string;
            gantt_compact: string;
            gantt_time: string;
            gantt_cook: string;
            gantt_timer: string;
            gantt_prep: string;
            gantt_empty: string;
            gantt_compressed: string;
            gantt_assembly: string;
            gantt_more_sections: string;
            gantt_section: string;
            gantt_mode_forward: string;
            gantt_mode_reverse: string;
            gantt_mode_target: string;
            gantt_target_time_label: string;
        };
        options: {
            scaleTitle: string;
            scaleGlobal: string;
            scaleByIngredient: string;
            selectIngredient: string;
            qty: string;
            unit: string;
            apply: string;
            bakersMath: string;
            enableBakersMath: string;
            bakersMathDesc: string;
            hideAbsolute: string;
            forceReference: string;
            autoDetect: string;
            physicalAnalysis: string;
            massStandardization: string;
            massDesc: string;
            yieldManagement: string;
            nutrition: string;
            stockTitle: string;
            stockDesc: string;
        };
        output: {
            copy: string;
            loading: string;
        };
        warnings: {
            consoleTitle: string;
            errors: string;
            error: string;
            warnings: string;
            warning: string;
            infos: string;
            info: string;
            item: string;
            show: string;
            expandConsole: string;
            collapseConsole: string;
        };
        errorState: {
            title: string;
            blockingDescription: string;
            jumpToError: string;
            line: string;
        };
        tabs: {
            addFile: string;
            removeFile: string;
            renameFile: string;
            entryFile: string;
            newFileName: string;
            previewingStandalone: string;
        };
        mobileTabs: {
            editor: string;
            preview: string;
        };
        analysisOptions: string;
        analysis: string;
        massNorm: string;
        yieldMgmt: string;
        nutritionEst: string;
        statusValid: string;
        statusWarnings: string;
        copyBtn: string;
    };
};
type GramLocale = typeof en;

declare function getDictionary(lang?: string): GramLocale;

export { CATEGORY_KEYS, type CategoryKey, type CompiledDict, type GramLocale, type LanguageDict, TIME_BY_LANG, TIME_DICTIONARIES, TIME_GLOBAL, TIME_TO_MINUTES, UNIT_BY_LANG, UNIT_CONVERSIONS, UNIT_DICTIONARIES, UNIT_GLOBAL, type UnitMap, compileDictionary, getAiLanguageInstruction, getCategoryLabels, getDefaultCategories, getDictionary, isCategoryKey, isCoarseUnit, normalizeUnit, resolveTimeUnit };
